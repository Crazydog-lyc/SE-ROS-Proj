# 测试报告

**项目名称**：SE-ROS-Proj  
**测试日期**：2026-06-18  
**测试环境**：Ubuntu 22.04 / ROS 2 Humble / Ignition Gazebo 6  
**报告版本**：2.0

---

## 1. 执行摘要

| 测试类别 | 计划用例 | 已执行 | 通过 | 通过率 |
|----------|----------|--------|------|--------|
| 单元测试 (nav2_mission_manager) | 78 pytest | 78 | 78 | 100% |
| 单元测试 (nav2_scenario_runner) | 2 gtest | 2 | 2 | 100% |
| 质量门禁 (flake8/bandit/coverage) | 1 | 1 | 1 | 100% |
| 集成/系统 (mission_demo) | 1 | 1 | 1 | 100% |
| 批量 batch (default profile) | 2 | 2 | 2 | 100% |

**总体结论**：单元测试与质量门禁全部通过；语句覆盖率 **92.8%**，超过 90% 门禁阈值。

---

## 2. 单元测试结果

### 2.1 nav2_mission_manager

**命令**：

```bash
bash src/nav2_mission_manager/scripts/run_quality_gate.sh
```

| 测试文件 | 结果 | 说明 |
|----------|------|------|
| `test_mission_loader.py` | PASS | JSON 加载与校验 |
| `test_state_machine.py` | PASS | 状态转移与安全事件 |
| `test_navigator_adapter.py` | PASS | Mock navigator 封装 |
| `test_retry_policy.py` | PASS | 重试策略 |
| `test_models.py` | PASS | 数据模型 |
| `test_mission_action_server.py` | PASS | Action 集成与安全备份链 |

### 2.2 质量门禁报告

**来源**：`src/nav2_mission_manager/test_reports/quality_gate_report.json`  
**生成时间**：2026-06-18

| 工具 | 结果 | 详情 |
|------|------|------|
| flake8 | **PASS** | 0 错误 |
| bandit | **PASS** | 0 high/medium |
| pytest+coverage | **PASS** | 语句 **92.8%**（阈值 90%）；分支 **87.8%**（阈值 60%） |

**总体**：`overall_passed: true`

### 2.3 nav2_scenario_runner

**命令**：

```bash
colcon test --packages-select nav2_scenario_runner
colcon test-result --verbose
```

| 测试文件 | 结果 |
|----------|------|
| `test_case_matrix_builder.cpp` | PASS |
| `test_metrics_utils.cpp` | PASS |

### 2.4 其他模块测试

| 包 | 测试 | 结果 |
|----|------|------|
| semantic_costmap_plugins | `test_geometry_utils.cpp`, `test_cost_functions.cpp` | gtest 通过 |
| sam_bot_safety_monitor | `test_safety_*.launch.py` | launch 测试通过 |
| sam_bot_nav2_gz | `test_*.launch.py` | launch 测试通过 |

---

## 3. 集成与系统测试

### 3.1 mission_demo（ST-01）

**配置**：`demo_pillar_room.sdf` + semantic + safety

| 检查项 | 结果 |
|--------|------|
| Gazebo 启动 | 通过 |
| SLAM + Nav2 lifecycle active | 通过 |
| RViz `/plan` 绿线 | 通过 |
| `/mission/run` 可接受 goal | 通过 |

### 3.2 单 Case launch（ST-02）

**Case**：`pillar_round_001`  
**Log**：`RESULTS_DIR/pillar_round_001.log`

| 检查项 | 结果 |
|--------|------|
| auto_run_mission 启动 | 通过 |
| Nav2 bt_navigator active | 通过 |
| Mission 状态机正常流转 | 通过 |
| Mission succeeded | 通过 |

### 3.3 批量测试（ST-03）

**脚本**：`src/nav2_scenario_runner/scripts/run_batch.sh`  
**Profile**：`default_batch.yaml`（2 个 pillar corridor case）

**配置示例**：

```bash
ENABLE_SEMANTIC=True ENABLE_SAFETY=True RUN_HEADLESS=False \
bash src/nav2_scenario_runner/scripts/run_batch.sh
```

| Case ID | nav2_ready | mission_succeeded | 结果 |
|---------|------------|-------------------|------|
| pillar_round_001 | 是 | 是 | PASS |
| pillar_round_002 | 是 | 是 | PASS |

**Batch 基础设施能力**：

| 能力 | 说明 |
|------|------|
| generate → run → summarize | 三步流水线完整 |
| `--tee-output` | 终端与 log 同步输出 |
| 进程清理 | case 间安全 cleanup，不误杀自身 |
| semantic overlay | 每 case 自动生成 `_semantic_overlay.yaml` |

---

## 4. 测试结论

1. **模块级**：mission 状态机、scenario 工具函数、语义几何/代价函数测试全部通过。
2. **质量门禁**：flake8、bandit、pytest 语句覆盖率 **92.8%**，整体门禁通过。
3. **系统级**：Gazebo + Nav2 + mission + safety + semantic 全栈 Demo 可稳定演示。
4. **批量级**：default batch 两轮 pillar 场景顺序执行通过，产出 `results.csv` 与独立 case log。

---

## 5. 附录：Log 关键字检查清单

| 关键字 | 含义 |
|--------|------|
| `Ready for navigation!` | Nav2 栈就绪 |
| `bt_navigator is active` | auto_run 前置条件满足 |
| `[MISSION][WAITING_FOR_RESULT]` | 等待 Nav2 完成当前航点 |
| `Mission succeeded:` | case 成功 |
| `Mission failed:` | case 失败 |
| `Local blockage detected` | safety 堵塞检测 |
| `Safety pause detected; backing up` | 安全暂停后退脱困 |
